import { ModulePlaceholder } from "@/components/ModulePlaceholder";

/**
 * Listado de inventario (en construcción — PASO 8).
 *
 * @returns Página de inventario.
 */
export function InventoryListPage() {
  return (
    <ModulePlaceholder
      title="Inventario"
      description="Materiales e insumos del taller con alertas de stock bajo y movimientos. En construcción."
    />
  );
}
